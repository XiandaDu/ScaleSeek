# verl 训练栈（SFT + RL）的依赖。
#
# requirements.txt 是旧集群上 eval 环境的 pip freeze，不包含这些 —— 那台机器
# 跑的是 baseline 评测，训练在别处做。verl 的 main_ppo / sft_trainer 缺了它们
# 直接 ImportError。
#
# 版本参考 grepseek 的 requirements-train.txt（同一份 vendored verl），
# 但不钉死 tensordict/torchdata：grepseek 用的是 torch 2.10，我们是 2.11，
# 让解析器按 torch 2.11 选兼容版本。
#
# 故意不采纳 verl/requirements.txt 里的 `numpy<2.0.0`：那是老 verl 留下的约束，
# torch 2.11 时代的整个栈都要求 numpy>=2，强行降级会连带弄坏 vllm 和 pyarrow。
#
# flash-attn 不在这里列出，但它是**硬依赖**，需要单独编译（见 scripts/build_flash_attn.sh）：
# 这份 vendored verl 的 FSDP engine 硬断言 pad_mode==NO_PADDING
# （verl/workers/engine/fsdp/transformer_impl.py:883），而 no_padding 走变长打包
# 序列，必须有 flash-attn 的 varlen 内核。没有它 SFT 在第一个训练步就
# AssertionError。之所以不写进本文件：PyPI 没有匹配 torch 2.11 的 wheel，必须用
# nvcc 从源码编，且要 MAX_JOBS=4 限流（grepseek TRAINING_ENV.md 记录 $(nproc) 会 OOM）。
ray[default]==2.54.0
tensordict
torchdata
codetiming==1.4.0
hydra-core==1.3.2
omegaconf==2.3.0
pylatexenc
pybind11
peft
liger-kernel
accelerate
wandb
tensorboard
latex2sympy2_extended
math_verify
